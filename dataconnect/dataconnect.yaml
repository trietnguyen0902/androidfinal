specVersion: "v1beta"
serviceId: "mail"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mail-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
